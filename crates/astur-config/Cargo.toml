[package]
name = "astur-config"
version = "2.1.1"
edition = "2021"
description = "Astur configuration parsing — shared by the WM and the settings GUI"
license = "Apache-2.0"

[lib]
path = "src/lib.rs"
